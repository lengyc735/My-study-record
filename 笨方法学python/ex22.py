str_function = '''
print()：打印输出。
len()：返回对象（如字符串、列表、字典等）的长度。
type()：返回对象的类型。
str(), int(), float()：进行类型转换。
input()：获取用户输入。
open()：打开文件。
range()：生成一系列的数字。
list(), dict(), set()：创建列表、字典和集合。
sorted()：返回排序后的列表。
max(), min(), sum()：返回最大值、最小值和总和。
'''
print(str_function)

str_caozuo = '''
赋值操作：如 x = 5，将值5赋给变量x。
算术操作：如加法+，减法-，乘法*，除法/，取余%，幂运算**等。
比较操作：如等于==，不等于!=，大于>，小于<，大于等于>=，小于等于<=等。
逻辑操作：如与and，或or，非not等。
成员操作：如in，not in，用于检查一个值是否在序列中。
身份操作：如is，is not，用于比较两个对象的身份（是否指向同一个对象）。
字符串操作：如字符串拼接+，字符串复制*，字符串切片等。
列表操作：如添加元素append()，删除元素remove()，pop()，列表切片等。
字典操作：如添加键值对，删除键值对，获取所有键keys()，获取所有值values()等。
文件操作：如打开文件open()，读取文件read()，写入文件write()，关闭文件close()等。
'''
print(str_caozuo)

str_knowloge = '''
数据类型：Python有多种数据类型，包括整数（int）、浮点数（float）、字符串（str）、列表（list）、元组（tuple）、字典（dict）和集合（set）等。

控制流：Python支持常见的控制流语句，包括if、for、while等，以及break和continue来控制循环的流程。

函数：Python使用def关键字来定义函数。函数可以接受参数，并且可以返回一个或多个值。

模块和包：Python使用import关键字来导入模块和包。Python有许多内置的模块，如math、datetime等，也有许多第三方的包，如numpy、pandas等。

错误和异常处理：Python使用try和except来处理错误和异常。

文件操作：Python可以读取和写入文件，包括文本文件和二进制文件。

面向对象编程：Python支持面向对象编程，可以定义类（class）和对象（object），并且支持继承和多态。

生成器和迭代器：Python支持生成器和迭代器，可以用于创建和处理大量的数据。
'''
print(str_knowloge)